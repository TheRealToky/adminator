# The report scripts (daily/weekly/monthly) are stdlib-only for terminal and
# --json output. The optional --docx flag, which writes a Word document, needs:
python-docx>=1.1.0

# The Excel import scripts (import_production, import_sales,
# update_reorder_thresholds) read/write .xlsx files with:
openpyxl>=3.1.0
